// Standalone layout for candidate intake — no dashboard chrome
export const metadata = {
  title: 'Candidate Information Form | IOTA Technologies',
  description: 'Secure candidate onboarding form — IOTA Technologies',
};

export default function CandidateIntakeLayout({ children }) {
  return children;
}
