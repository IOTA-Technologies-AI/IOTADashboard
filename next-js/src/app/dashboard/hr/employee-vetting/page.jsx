import VettingListWrapper from './list-wrapper';

// ----------------------------------------------------------------------

export const metadata = { title: `Employee Vetting` };

export default function Page() {
  return <VettingListWrapper />;
}
