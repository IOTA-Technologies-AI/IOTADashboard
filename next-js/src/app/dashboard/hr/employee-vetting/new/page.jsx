import VettingNewWrapper from './new-wrapper';

// ----------------------------------------------------------------------

export const metadata = { title: `New Employee Vetting` };

export default function Page() {
  return <VettingNewWrapper />;
}
