import { PipelineView } from 'src/sections/sales/view';

// ----------------------------------------------------------------------

export const metadata = { title: `Sales | Pipeline` };

export default function Page() {
  return <PipelineView />;
}
