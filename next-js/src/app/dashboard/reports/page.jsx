import { OverviewReportsView } from 'src/sections/overview/reports/view';

// ----------------------------------------------------------------------

export const metadata = { title: 'Reports | P&L' };

export default function Page() {
  return <OverviewReportsView />;
}
