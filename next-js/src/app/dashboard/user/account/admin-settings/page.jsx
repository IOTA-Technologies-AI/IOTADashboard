import { AccountAdminSettingsView } from 'src/sections/account/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: `Admin settings`,
};

export default function Page() {
  return <AccountAdminSettingsView />;
}
