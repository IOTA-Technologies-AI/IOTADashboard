import { CONFIG } from 'src/global-config';

import { AvatarView } from 'src/sections/_examples/mui/avatar-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Avatar | MUI` };

export default function Page() {
  return <AvatarView />;
}
